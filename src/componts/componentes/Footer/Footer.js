import classes from './Footer.module.css'
const Footer = (props) => {
    return (
        <div className={classes.footer}>
            <p>Create account and login on facebook</p>
        </div>
    )
}
export default Footer;